[project]
name = "panthguru"
version = "1.0.0"
description = "PanthGuru — The Travel planner. An AI-powered agentic travel planner built with LangGraph."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "langchain>=1.3.11",
    "pandas>=3.0.3",
]
